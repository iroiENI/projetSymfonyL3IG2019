{% extends 'base.html.twig' %}

{% block title %}Les produits
{% endblock %}

{% block body %}

    <div class="container-fuid text-center" style="margin-top: 80px; padding-top:20px; background-color:rgb(205, 218, 218);">
        {{ form_start(form) }}
        <h3 class="text-left" style="margin-left: 60px;">Les Categories</h3>

        <div class="form-inline">
                <div class="form-group mb-2" style="margin-left: 20px;">
                        {{ form_row(form.category,  {'label': ' ', 'id': 'edit'}  ) }}
                </div>
                <div class="form-group mx-sm-3 mb-2">
                    {{ form_row(form.designation,  {'label': ' ','attr': {'placeholder': 'Libelle du produit'}}) }}
                </div>
                <button type="submit" class="btn btn-primary mb-2">rechercher</button>
        </div>
        {{ form_end(form)}}
    </div> 
  
    {#<div class="col-lg-12">

            <div id="carouselExampleIndicators" class="carousel slide my-4" data-ride="carousel">
              <ol class="carousel-indicators">
                <li data-target="#carouselExampleIndicators" data-slide-to="0" class="active"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="1"></li>
                <li data-target="#carouselExampleIndicators" data-slide-to="2"></li>
              </ol>
              <div class="carousel-inner" role="listbox">
                <div class="carousel-item active">
                  <img class="d-block img-fluid" src="http://placehold.it/1500x350" alt="First slide">
                </div>
                <div class="carousel-item">
                  <img class="d-block img-fluid" src="http://placehold.it/1500x350" alt="Second slide">
                </div>
                <div class="carousel-item">
                  <img class="d-block img-fluid" src="http://placehold.it/1500x350" alt="Third slide">
                </div>
              </div>
              <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                <span class="sr-only">Previous</span>
              </a>
              <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                <span class="carousel-control-next-icon" aria-hidden="true"></span>
                <span class="sr-only">Next</span>
              </a>
            </div>#}

    <div class="bg-light p-3 text-center" style="margin-top: 20px;">
        <h1>Nos produits </h1>
    </div>

    <div class=" container row">
    {% for product in produit %}

        <div class="col-lg-3 col-md-6 mb-4">
            <div class="card h-300">
              <a href="{{ path('home_show', {'id': product.id}) }}"><img class="card-img-top" src="{{ product.image }}" alt=""></a>
              <div class="card-body">
                <h5> {{ product.pu }} Ar</h5>
                <p class="card-text">{{ product.designation }}</p>
              </div>
              <div class="card-footer">
                    {% if app.user %}
                    <a href="{{ path('panier_add', {'id': product.id}) }}" class="btn btn-success float-right">
                        <i class="fas fa-shopping-cart mr-1"></i>
                        Ajouter
                    </a>
                    {% else %}
                        <a href="{{ path('security_connexion') }}" class="btn btn-success float-right">
                            <i class="fas fa-shopping-cart mr-1"></i>
                            Ajouter
                        </a>

                    {% endif %}
              </div>
            </div>
          </div>
        
        {#
            <div class="col-4 mt-4">
                <div class="border border-dark rounded p-2 clearfix">
                    <h2>
                        {{ product.Designation }}
                        -
                        {{ product.pu }}
                        Ar
                    </h2>
                    <img class="img-fluid rounded" src="{{ product.image }}" alt="Image de {{ product.Designation }}">
                    <hr>
                    {% if app.user %}
                    <a href="{{ path('panier_add', {'id': product.id}) }}" class="btn btn-success float-right">
                        <i class="fas fa-shopping-cart mr-1"></i>
                        Ajouter
                    </a>
                    {% else %}
                        <a href="{{ path('security_connexion') }}" class="btn btn-success float-right">
                            <i class="fas fa-shopping-cart mr-1"></i>
                            Ajouter
                        </a>

                    {% endif %}
                </div>

            </div>#}
        {% endfor %}
    </div>
{% endblock %}
